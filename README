Automated rsync wrapper and web UI.


Features:

- synchronize data between hosts
- allow both remote source and destination
- discover lan hosts and partitions
- find hosts from a username or a partition uuid or a mac address
- mount partitions
- create destination path


Start the daemon:
./syncd/run.py

Start the webui:
./run.py
